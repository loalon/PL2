/* constantes  */

#define TRUE 1;
#define FALSE 0;
#define UNO 1;

struct Cuadrado {
	int cX;
	int cY;
}

int a;

Cuadrado cuadrado, cuad, cosa;


void main () {
	
	a=3-1;
	if(a==2) {
		printc("correcto");
	}
else {
		printc("incorrecto");
	}
	/*
	printc("CONSTANTES");
	printc("uno(1):");
        printi(UNO);
        printi(a);
        */

}